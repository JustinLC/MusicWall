create a model
plan the columns and associations needed

sign 
song_id
user_id

<form action

def liked(:id)
	@songs = Songs.find
	@like = song.likes.find(current_user)
	if relike and @liked.value == true
		like.displike
	else
		@like.like
	end
end